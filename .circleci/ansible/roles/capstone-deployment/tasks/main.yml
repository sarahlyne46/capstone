- name: Copy deployment configuration
  copy:
    src: "app-blue.yml"
    dest: $HOME

- name: Check deployments
  shell: "./bin/kubectl get deployments"
  args:
    chdir: $HOME

- name: Check pods
  shell: "./bin/kubectl get pods"
  args:
    chdir: $HOME

- name: Describe pods
  shell: "./bin/kubectl describe pod"
  args:
    chdir: $HOME
    
- name: Apply deployment configuration
  shell: "./bin/kubectl apply -f app-blue.yml"
  args:
    chdir: $HOME
